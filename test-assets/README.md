# Test Assets

Store reusable fixtures for automated tests and manual QA here.

Examples:

- sample story briefs or structured workflow payloads
- audio fixture clips
- image fixtures for UI tests
- exported artifacts used by regression tests

Keep generated runtime artifacts out of this directory unless they are intentional, versioned test fixtures.
