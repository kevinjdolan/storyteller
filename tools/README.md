# Tools

This directory holds repository-specific tooling that is not part of the production application runtime.

Current contents:

- `webapp-qa/`: browser automation tooling used for smoke checks and screenshots
