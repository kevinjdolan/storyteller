Curated local background music loops for the narration mixer.

The `.wav` files in this directory are generated from
`generate_curated_music.py` so the repo does not depend on third-party music
licenses or network fetches. Regenerate them with:

```bash
python backend/app/data/background_music/generate_curated_music.py
```
