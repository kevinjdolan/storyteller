# Backend Worker

This package is reserved for durable background job execution.

Future prompts should place composition, narration, and other long-running workflow workers here instead of embedding that logic in HTTP route handlers.
